export interface ServiceCard {
  klasse: string;
  label: string;
  beschreibung: string;
  details?: string[];
  preis?: string;
  icon?: string;
}

export interface USPItem {
  title: string;
  description: string;
  icon: string;
}
